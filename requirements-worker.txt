# Deps for the Celery worker container only - deliberately NOT the full
# requirements.txt (langchain/chromadb/ragas/jupyter/etc aren't needed here
# and would make the worker image huge and slow to build). Add to this list
# only as tasks actually need something - e.g. sqlalchemy+psycopg2 the day a
# task first touches the database.
celery>=5.3.0
redis>=5.0.0
httpx>=0.27.0
pydantic-settings>=2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
